Update the `battery.menu` file in:
```
/System/Library/CoreServices/Menu Extras/
```

Optional:
Rename and keep the old (current) `battery.menu` file as a backup

Run `killall SystemUIServer` in terminal to reset the menubar
